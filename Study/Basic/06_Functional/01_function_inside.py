# 내장 함수

"""
abs(x) : 절대값

chr(i) : 아스키 코드 값 -> 문자

divmod(a, b) : a를 b로 나눈 몫과 나머지의 튜플 형태

eval(expression) : 문자열을 실행한 결과값

hex(x) : 정수값을 16진수로 변환

id(object) : 객체의 주소 값

len(s) : s의 길이 or 요소 전체의 개수

list(s) : 반복 가능한 자료형 s를 list로 변환

map(f, iterable)
: 함수 f와 반복 가능한 자료형 iterable을 받아 각 요소를 f가 수행한 결과를 묶어 반환

max(iterable) : 반복 가능한 자료형의 최대 값

min(iterable) : 반복 가능한 자료형의 최소 값

open(filename) : 파일 이름을 받아 파일 객체를 반환

range(start, stop, step) : 주로 for문과 함께 사용, 범위 값을 반복가능한 객체로 반환

str(object) : 문자열 형태로 객체를 변환
"""